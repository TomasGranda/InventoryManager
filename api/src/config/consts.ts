const rootPath = '/api';

// Items Routes
export const itemPath = `${rootPath}/items`;
export const itemIdParam = 'itemId';

// ItemTypes Routes
export const itemTypePath = `${rootPath}/itemtypes`;
export const itemTypeIdParam = 'itemTypeId';
